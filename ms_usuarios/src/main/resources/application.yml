server:
  port: 8081

spring:
  application:
    name: ms-usuarios

  datasource:
    url: jdbc:oracle:thin:@bdnovalogistic_high?TNS_ADMIN=/app/Wallet_BDNOVALOGISTIC
    username: ADMIN
    password: Novalogistic4590
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# Solución para el conflicto de dependencias de HATEOAS con OpenAPI
springdoc:
  show-hateoas: false
  api-docs:
    path: /v3/api-docs
