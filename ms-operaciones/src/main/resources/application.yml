server:
  port: 8084

spring:
  application:
    name: ms-operaciones
  
  datasource:
    url: jdbc:oracle:thin:@bdnovalogistic_low?TNS_ADMIN=C:/Users/Juan/Music/Nueva-carpeta
    username: ADMIN
    password: Novalogistic4590
    driver-class-name: oracle.jdbc.OracleDriver
    flyway:
      enabled: true
      locations: classpath:db/migration
  
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  enable-hateoas: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/